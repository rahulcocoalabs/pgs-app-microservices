const MovieActor = require('../models/movieActor.model.js');

function movieActorController(methods, options) {
this.listAll = (req, res) => {
    console.log("listAll() working...");
};
};
module.exports = movieActorController; 